# Sample YAML for client config
name: "Ecom Client"
version: "1.0"
client:
  organization: Ecom
  credentialStore:
    path: "./api/sdk/wallet/ecom/admin"
    cryptoStore:
      path: "./api/sdk/wallet/ecom/admin"
  connection:
    options:
      grpc.keepalive_time_ms: 120000